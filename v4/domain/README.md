# V4 domain layer

- `rules.js`: thresholds and business classifications.
- `diagnostics.js`: Business Health and alerts.
- `../state/store.js`: shared local application state.
- `../engines/`: centralized economic calculations.

The UI must consume these layers rather than maintain competing formulas.
